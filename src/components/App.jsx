import ImageContainer from "./ImageContainer";

function App() {
    // Fetch from GET /photo and pass it to ImageContainer
    // Use Context?

    return (
        <main>
            <ImageContainer />
        </main>
    );
}

export default App;
